// create 2D array or list with letters

// filter function that lists all words of length n from 2D array/list (vertical, horizontal, diagonal [8 directions in total])

// for all positions
// for all directions
// - go in dir until n elements are reached
// - if not enough -> None
// - else n elements

// filter words that contain letter at specific position